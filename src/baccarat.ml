(* open Deck

   let baccarat () = print_endline "How much money would you like to
   bet?"; let command = read_line () in let new_deck = Deck.make_deck in
   let player_hand = Deck.pull_card new_deck 2 in let dealer_hand =
   Deck.pull_card new_deck 2 in *)
